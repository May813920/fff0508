{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "streamlit run app.py --server.address=0.0.0.0 --server.port=8080 --server.headless=true",
    "restartPolicyType": "ALWAYS"
  }
}
